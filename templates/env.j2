#!/bin/sh

export PATH="{{ home_path }}/.plenv/bin:$PATH";
export PLENV_ROOT="{{ home_path }}/.plenv";
export HOME="{{ home_path }}"
eval "$({{ home_path }}/.plenv/bin/plenv init -)";

cd $(dirname $0)

exec "$@"
